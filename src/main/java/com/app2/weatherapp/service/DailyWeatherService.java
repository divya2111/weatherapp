package com.app2.weatherapp.service;
import com.app2.weatherapp.model.WeatherS;
import java.util.ArrayList;
import java.util.List;

import org.springframework.stereotype.Service;
@Service
public class DailyWeatherService {






   /* // Simulate fetching weather data for a specified number of days
    public List<WeatherS> getWeatherForDays(int days) {
        List<WeatherS> weatherList = new ArrayList<>();
        
        // Generate mock weather data for the specified number of days
        for (int i = 1; i <= days; i++) {
            WeatherS weather = new WeatherS("Day " + i, 20 + i, 50 - i, 10 + i, "Sunny");
            weatherList.add(weather);
        }

        return weatherList;
    }*/
}
